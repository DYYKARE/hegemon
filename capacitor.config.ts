import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yusuf.hegemon',
  appName: 'Hegemon',
  webDir: 'dist',
  android: {
    backgroundColor: '#020617',
  },
};

export default config;
